/**
 * t_comienzo = System.currentTimeMillis(): //CAPTURO INSTANTE INICIAL
 * insercionComun(hsPos);
 * t_fin = System.currentTimeMillis(); //CAPTURO INSTANTE FINAL
 * t_total = t_fin - t_comienzo; //RESTO TIEMPO Y OBTENGO MS TOTALES
 *
 * Construye un programa que determine cual de las 3 colecciones conjunto vistas en el capitulo
 * (HashSet, LinkedHashSet o TreeSet) es mas rapida a la hora de añadir elementos al conjunto, comprobar
 *
 */
public class Main {
    public static void main(String[] args) {
        System.out.println("Hello world!");
    }
}